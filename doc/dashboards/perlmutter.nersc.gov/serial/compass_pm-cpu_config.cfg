[paths]

# A root directory where MPAS test data will be downloaded to
database_root = /pscratch/sd/m/mcarlson/biweeklyCDashPerlmutter-serial/repos/mpas-data

# The parallel section describes options related to running tests in parallel
[parallel]

# parallel system of execution: slurm or single_node
system = single_node

# whether to use mpirun or srun to run the model, plus options, e.g. "--use-hwthread-cpus"
parallel_executable = srun