#ifndef ALBANY_PUBLICTRILINOSTRICKERY_HPP
#define ALBANY_PUBLICTRILINOSTRICKERY_HPP

// Trickery to get Albany to continue to build against stale publicTrilinos. So
// far I haven't been able to centralize the ALBANY_USE_PUBLICTRILINOS code very
// much, but this file is here for that when it's possible.

#include <boost/mpl/vector.hpp>
#include <boost/mpl/find.hpp>
#include <boost/mpl/map.hpp>
#include <boost/mpl/placeholders.hpp>

#endif // ALBANY_PUBLICTRILINOSTRICKERY_HPP
