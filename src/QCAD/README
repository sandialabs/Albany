This folder used to contain the QCAD physics code
The QCAD package has been discontinued due to the lack
of resources to maintain it. You can retrieve it by
checking out the QCAD_support_end tag:

git checkout tags/QCAD_support_end

You can find information on the trilinos revision to use
in the tag comment. Simply do

git tag -v QCAD_support_end

to visualize information.
